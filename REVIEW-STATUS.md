# REVIEW-STATUS — статус находок REVIEW-P1P2.md

Обновлено: 2026-09-15 (после коммитов `bca7708`..HEAD + commit-а robots/sitemap/PRD).
Исходный отчёт: `REVIEW-P1P2.md` (2026-09-11, 34 находки). Статусы: ✅ закрыто / ♻️ устарело (контекст ушёл) / ⏸️ осознанно отложено.

## A. Покрытие требования заказчика

| ID | Статус | Примечание |
|---|---|---|
| A1 | ✅ | Деплой: прод = LaunchAgent `com.tech-diagnostics.server`; после коммитов — `launchctl kickstart -k` + smoke (закреплено в SESSION_STATUS.md). |
| A2 | ✅ | Сделано этим документом + SESSION_STATUS.md (обзор всего проекта). |
| A3 | ✅ (в другой форме) | «Третий вариант» (своя форма) удалён (`680a1a3`): `/support` — уведомление + кнопка «Подтверждаю — перейти на сайт ASUS» (вариант «просто ссылка» из требования). Согласование с владельцем де-факто пройдено. |
| A4 | ⏸️ | Deep-link предзаполнения формы ASUS не исследовался. Форма ASUS удалена из сайта; переход — на стартовую questionform. Если нужно предзаполнение — отдельная задача. |
| A5 | ✅ | Конфликта нет. |

## B. Гонки P1/P2

| ID | Статус | Примечание |
|---|---|---|
| B1 | ✅ | Сборку выполняет только оркестратор; агентов больше нет. |
| B2 | ✅ | Токены в `globals.css` применены. |
| B3 | ✅ | `layout.tsx` — владелец оркестратор; шрифт-плейсхолдер заменён на PNG-логотипы (`logo-day/night.png`), `public/fonts/` удалён. |
| B4 | ♻️ | Миграции: `0000`–`0009` (+`0010` при новых). Ограничение на одного исполнителя соблюдается процессом. |
| B5 | ♻️ | Белые списки путей — не нужны без параллельных агентов. |
| B6 | ⏸️ | Отдельные dev-БД при dev-прогонках (паттерн в SESSION_STATUS.md); миграция в прод — при остановленном LaunchAgent (kickstart делает это атомарно). |
| B7 | ♻️ | Единый набор проверок: tsc/eslint/build/vitest (+headless) — в SESSION_STATUS.md. |
| B8 | ♻️ | `lib/support.ts` сужен до констант URL; `tests/` — 44 теста по живой логике. |
| B9 | ✅ | `SupportFormStub.tsx`, `Logo.tsx.bak` удалены. |
| B10 | ✅ | `PROJECT_STRUCTURE.md`/`PRD.md` обновляются по мере правок (этап 2.2 добавлен). |

## C. Лендинг и эталон

| ID | Статус | Примечание |
|---|---|---|
| C1 | ✅ | Проверено (см. отчёт). |
| C2 | ✅ | `TicketSearch.tsx`: форма ввода номера на `/ticket` без параметра; при неверном номере — «не найдено» + та же форма. Headless-проверено. |
| C3 | ♻️ | Curl-список в спеке; сейчас headless-прогон всех страниц (скрипты `/tmp/pwtest/`). |
| C4 | ✅ | `PRD.md` — этап 2.2 «редизайн лендинга под эталон ASUS» (2026-09-15). |
| C5 | ✅ | Зафиксировано в этапе 2.2 PRD («эталон показывает только первый экран, нижние секции сохранены по PRD»). |
| C6 | ⏸️ | SVG-эталон печатной платы не прикладывался; реализация принята визуально. |
| C7 | ✅ | Подписи «сервис вычислительной техники» под логотипом нет (grep 0 совпадений) — соответствует эталону. |

## D. Вордмарк

| ID | Статус | Примечание |
|---|---|---|
| D1 | ⏸️ | Плейсхолдер = официальные PNG-логотипы AS-RUSSIA (`logo-day/night.png`) — вопрос кастомного шрифта снят. |
| D2 | ✅ | `public/fonts/Michroma-Regular.ttf` удалён. |
| D3 | ✅ | Логотипы — PNG с прозрачным фоном, `object-contain`, `h-7/h-8`. |

## E. Тесты и верификация

| ID | Статус | Примечание |
|---|---|---|
| E1 | ✅ | PNG-артефакты ревью: `/tmp/td-review/` (все страницы, 1440 light/dark + 375 + карта). ⚠️ /tmp не вечно — при необходимости переснять. |
| E2 | ✅ | Прогон 375×812 всех страниц: горизонтального overflow нет (единственный выпирающий элемент — hero-дек, клипнут `overflow-hidden`). Карта: подписи городов СЦ теперь под пинами (2026-09-15). |
| E3 | ✅ | `.dark`: `--accent-fill-text: #0b1220`, `--accent-fill-border: #31708f`; в дереве 0 совпадений `bg-accent text-white`. |
| E4 | ✅ | Глобальный `:focus-visible` в `globals.css`; Escape-закрытие меню — не проверялось отдельно (⏸️). |
| E5 | ♻️ | Публичный POST `/api/support/request` удалён (680a1a3). Осталась rate limit логина (5/10 мин/IP) и warranty (10/10 мин/IP). |
| E6 | ♻️ | Валидация в `lib/support.ts` удалена вместе с формой. |
| E7 | ✅ | ПДн не собираются: таблица удалена (0008), `/privacy` переписан, акценты убраны. |
| E8 | ♻️ | `support_requests` удалена; коллизия номеров невозможна. |
| E9 | ♻️ | API поддержки удалено. |
| E10 | ⏸️ | Сквозной прогон `/support` — `support-check.mjs` (route-перехват, asus.com). Clipboard-фичи нет. |
| E11 | ✅ | Ручные/headless-прогоны: чат до тикета, гарантия до результата, карта (тайлы/pins/popups/OCR), админка (tree-admin), PDF (print-check). |
| E12 | ⏸️ | Базовый уровень lint warnings не зафиксирован. |
| E13 | ✅ | Прогон всех страниц в dark — скриншоты + чистые консоли (2026-09-15). |
| E14 | ✅ | Консоли браузеров (pageerror + console.error) чистые на всех страницах, light и dark. |
| E15 | ✅ | `app/robots.ts` + `app/sitemap.ts` + `metadataBase` (2026-09-15); прод отдаёт 200 на `/robots.txt` и `/sitemap.xml`. |

## Карта /centers — диагностика «нет названий городов» (2026-09-15)

Симптом: на дефолтном зуме «вся Россия» (fitBounds ~z8–9) города почти не видны.
Причины (не баг конвейера): 1) OSM place-данные на зуме «вся Россия» редки; 2) ромб-пин СЦ стоит на месте города и **перекрывает** векторную подпись (Москва пропала, без пинов — видна).
Проверено: glyph-запросы 200 (вкл. кириллицу 1024-1279), ошибок 0, страна/города рендерятся (OCR: «Москва», «Пекин» без пинов; «Екатеринбург», «Уфа», «Омск» на зум+1..2).
Фикс: подпись города СЦ под каждым пином (`.map-pin-city`, данные `city` из БД) — 51 подпись, light+dark, headless+OCR.
